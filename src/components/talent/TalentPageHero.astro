---
interface Props {
  eyebrow: string;
  title: string;
  description: string;
}

const { eyebrow, title, description } = Astro.props;
---

<header class="mockup-hero section-reveal">
  <div class="mockup-hero__container">
    <p class="mockup-hero__eyebrow">{eyebrow}</p>
    <h1 class="mockup-hero__title">{title}</h1>
    <p class="mockup-hero__description">{description}</p>
  </div>
</header>
